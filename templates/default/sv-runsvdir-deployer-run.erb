#!/bin/bash
exec 2>&1
exec chpst -u deployer -e <%= "#{node['dotpromo-rails-box']['app_dir']}/runit/.env" %> runsvdir <%= "#{node['dotpromo-rails-box']['app_dir']}/runit/enabled" %> 'log: ................................................................................................................................................................................................................................................................'
